[project]
name = "imdb-scraper"
version = "0.1.0"
description = "Scrape IMDb movie data by release year using Selenium and BeautifulSoup."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "beautifulsoup4>=4.12.0",
    "lxml>=5.0.0",
    "pandas>=2.0.0",
    "selenium>=4.20.0",
]

[tool.uv]
package = false
